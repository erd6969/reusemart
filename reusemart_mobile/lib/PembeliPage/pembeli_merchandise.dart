import 'package:flutter/material.dart';

class PembeliMerchandisePage extends StatelessWidget {
  const PembeliMerchandisePage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text(
          'Ini adalah halaman dummy Pembeli Merchandise',
          style: TextStyle(fontSize: 18),
        ),
      ),
    );
  }
}
